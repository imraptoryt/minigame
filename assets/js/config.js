// ============================================================================
// Fill these in from Supabase → Project Settings → API, then deploy.
// The anon key is safe to expose client-side: every table is protected by
// Row Level Security (see supabase/schema.sql), so the anon key alone can't
// read or write anything outside what a signed-in user is allowed to.
// ============================================================================
window.LSC_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
